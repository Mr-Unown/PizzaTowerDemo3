/// @Description Checks the Object's State
/// @param object
with argument0
{
	
if state == 24 || state == 27 || state == 18 || state == 25 || state == 15 || state == 11 || state == 13 || state == 14 || state == 86
	return true;
else
	return false;
}